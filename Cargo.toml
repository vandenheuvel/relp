[package]
name = "rust-lp"
version = "0.0.2"
authors = ["Bram van den Heuvel <bram@vandenheuvel.online>"]
license = "MIT"
repository = "https://github.com/vandenheuvel/rust-lp"
description = "A solver for linear programs"

edition = "2018"

[dependencies]
num = "0.3.x"
daggy = "0.6.x"  # Currently only for debug asserts

itertools = "0.9.0"

[features]
miplib = []
burkardt = []
